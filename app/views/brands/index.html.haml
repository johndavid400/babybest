%h3 Brands

.pull-right= link_to "New Brand", new_brand_path, :class => "button secondary"

%hr

- @brands.each do |brand|
  .row
    .large-12.columns
      = link_to brand.name, brand_path(brand), :class => "button expand"

