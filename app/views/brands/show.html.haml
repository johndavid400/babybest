%h1= @brand.name

.pull-right= link_to "New Product", new_product_path, :class => "button secondary"

.row
  .large-12.columns
    - @brand.products.each do |product|
      = link_to product.name, product_path(product), :class => "button expand"
