%h1= @company.name

.pull-right= link_to "New Product", new_product_path, :class => "button secondary"

.row
  .large-12.columns
    - @company.products.each do |product|
      = link_to product.name, product_path, :class => "button expand"
